
def test_model(model: str, system_prompt: str, user_prompt: str):
    # Run the model on test set and save results
    # Evaluate accuracy of ingredients and confidence score
    # Store accuracy, cost, and latency metrics for each model
    pass
